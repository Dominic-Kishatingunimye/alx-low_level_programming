#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen -> a function that returns the length of a string
 * *s - pointer to parameter
 * Return: always 0
 */
int _strlen(char *s)
{
	int len;

	len = strlen(str);
	printf(" %d\n", len);
	return (0);
}
